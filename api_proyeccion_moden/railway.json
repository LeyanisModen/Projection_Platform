{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": [
      "python manage.py migrate --noinput"
    ],
    "startCommand": "sh -c 'python manage.py collectstatic --noinput && exec gunicorn proyeccion_moden.wsgi --bind 0.0.0.0:8000 --workers=3 --threads=4 --timeout=120 --keep-alive=5 --log-file=-'"
  }
}
