{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": [
      "python manage.py migrate --noinput"
    ],
    "startCommand": "sh -c 'python manage.py collectstatic --noinput && exec gunicorn proyeccion_moden.wsgi --bind [::]:8000 --workers=${WEB_CONCURRENCY:-2} --threads=${GUNICORN_THREADS:-4} --timeout=120 --keep-alive=5 --log-file=-'"
  }
}
